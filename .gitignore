# User config with credentials (POC config.json — C# app stores in AppData)
config.json
__pycache__/
*.pyc
.pytest_cache/
.worktrees/

# Spec-kit local artefacts — kept on disk but not in the repo
/.specify/
/specs/

# Visual Studio / .NET build
.vs/
bin/
obj/
publish/
*.user

# CI packaging output
/artifacts/
/dist/
EcoFlowMonitor-*.zip
EcoFlowMonitor-*.tar.gz

# Diagnostic scripts with hardcoded credentials
poc/mqtt_diag.py

# Claude Code local state (worktrees, settings.local.json, plans, hooks, etc.)
.claude/
